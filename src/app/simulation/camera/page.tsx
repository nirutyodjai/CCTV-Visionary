'use client';

import React from 'react';
import { CameraViewSimulator } from '@/components/simulation/camera-view-simulator';

export default function CameraSimulationPage() {
  return (
    <div className="h-full">
      <CameraViewSimulator />
    </div>
  );
}
